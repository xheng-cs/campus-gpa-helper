# 📋 项目设计文档：GPA+ 校园学业助手

> 完整流程：**想法 → AI 开发 → 调试 → Git 提交 → 完成交付**
> 这份文档就是「想法」阶段的产物：先想清楚再动手。

## 1. 想法：为什么做这个项目

- **场景**：大学生要经常算绩点、查课程、记作业。手机 App 要么有广告，要么数据要传服务器；教务处系统又不好用。
- **目标**：做一个**零安装、零依赖、打开即用**的网页小工具，数据只保存在自己的浏览器里。
- **意义**：对同学有用；对我自己是一次完整的工程实践（分层设计、单元测试、Git、CI），也是面试的展示项目。

## 2. 功能设计（MVP）

1. **绩点计算器**：支持 4.0 分档制 / 5.0 连续制两种算法，一键切换；成绩支持百分制（92、88.5）和等级制（优秀/良好/中等/及格/不及格）。
2. **课程管理**：添加/删除课程（课程名、学分、成绩），实时计算总学分、加权平均分、GPA。
3. **作业待办**：任务名、所属课程、截止日期；勾选完成；过期/今天截止/未来 自动高亮。
4. **本地持久化**：localStorage 存储，刷新不丢；支持导出 JSON、载入示例数据。
5. **体验细节**：跟随系统深色模式、移动端响应式。

## 3. 技术选型（为什么这么选）

| 选择 | 理由 |
| --- | --- |
| 原生 HTML / CSS / JavaScript | 零依赖、零构建，双击就能运行，也最适合理解浏览器底层 |
| 经典脚本 + `window.GPA` 命名空间（UMD-lite） | ES 模块在 `file://` 下会被浏览器 CORS 拦截；为了「双击即用」采用经典脚本，同时兼容 Node `require` |
| 纯函数核心（`gpa.js`）与 UI（`app.js`）分离 | 计算逻辑不碰 DOM，可以脱离浏览器直接单元测试 |
| Node 内置 test runner（`node --test`） | 不用安装 Jest 等任何依赖 |
| GitHub Actions | 每次 push 自动运行测试，养成 CI 习惯 |

## 4. 数据模型

- 课程 `Course`：`{ name: string, credits: number, score: number }`（score 统一存百分制）
- 待办 `Todo`：`{ id: string, title: string, course: string, due: string|null, done: boolean }`
- 存储键：`gpaplus.courses` / `gpaplus.todos` / `gpaplus.mode`

## 5. 绩点算法

- **4.0 分档制**（默认，参考美式）：90+→4.0，85+→3.7，82+→3.3，78+→3.0，75+→2.7，72+→2.3，68+→2.0，64+→1.7，60+→1.0，<60→0
- **5.0 连续制**：绩点 = (分数 − 50) / 10，低于 60 记 0
- **等级制映射**：优秀→95 · 良好→85 · 中等→75 · 及格→65 · 不及格→0
- **总 GPA** = Σ(课程绩点 × 学分) ÷ Σ学分；**加权平均分** = Σ(分数 × 学分) ÷ Σ学分
- 算法集中在 `js/gpa.js` 顶部的映射表里，不同学校的政策改一行即可

## 6. 测试计划

- 成绩解析：百分制 / 带「分」后缀 / 等级制 / 非法输入（负数、超 100、乱码）
- 算法边界：4.0 分档的 90 / 89.9 / 60 / 59.9；5.0 的 100 / 60 / 59
- 汇总计算：加权平均与 GPA 的公式正确性；空列表返回 null；脏数据过滤
- 浮点边界：四舍五入的精度（这是最容易出 bug 的地方）

## 7. Git 提交规范

- 每完成一个独立主题提交一次，前缀用 `feat` / `fix` / `docs` / `test` / `ci`
- 保证**每个提交测试都是绿的**，历史干净可回溯
